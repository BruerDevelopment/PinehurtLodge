
const HOSTNAME = "Pinehurst-Lodge.com";
const BASE_PATH = "";
export const CONFIG = {
    HOSTNAME,
    BASE_PATH,
    BASE_URL: `https://${HOSTNAME}${BASE_PATH}`,
    page_meta: {
        title: "Pinehurst Lodge",
        description:""
    }
}